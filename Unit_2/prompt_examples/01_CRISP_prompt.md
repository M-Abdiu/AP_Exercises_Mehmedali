C (Context): Python 3.14. File src/stats.py. Function median(nums: list[float]) -> float (see code).
R (Request): Fix bug: lists return the wrong median.
I (Invariants): Keep signature + docstring style. Don’t add dependencies. Handle exceptions.
S (Safety checks): Add pytest for edge cases such as duplicates, empty list.
P (Presentation): Output python code with documentation in PEP 257 format.
